class Collection {
  //final int? id;
  //final String? name;
  //final String? description;
  //final String? status;

  //Collection({this.id, this.name, this.description, this.status});

  /*
  factory Collection.fromJson(Map<String, dynamic> json) {
    return Collection(
      id: json["id"],
      name: json["name"],
      description: json["description"],
      status: json["status"],
    );
  }
  */
}
